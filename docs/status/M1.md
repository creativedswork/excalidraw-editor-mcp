# M1 状态

> 当前状态：`MILESTONE_CANDIDATE`
>
> 执行状态：`AWAITING_ACCEPTANCE`

## Slice 台账

| Slice | 状态 | 结果 | Commit |
|---|---|---|---|
| S1 | `COMMITTED_LOCAL` | 安全 Workspace、标准画布存储、revision、CAS、原子写和幂等 | `6743241` |
| S2 | `COMMITTED_LOCAL` | 受管/发现工程 lifecycle | `5b6407c` |
| S3 | `COMMITTED_LOCAL` | 工程内画布 lifecycle | `ec8f607` |
| S4 | `COMMITTED_LOCAL` | MCP 工具分层、可信 Workspace context、无 View stdio 与 packed smoke | `6cec45c` |
| F1 | `COMMITTED_LOCAL` | 补齐实际 DSH 配置、四个用户验收用例和 Browser blocker 证据 | 本提交 |
| F2 | `COMMITTED_LOCAL` | 修正 Host MCP App resource 上限并复验真实画布渲染 | 本提交 |
| F3 | `COMMITTED_LOCAL` | 从 invoking tool result 拉取并加载绑定画布 | 本提交 |

## 范围

- 只修改 `excalidraw-editor-mcp`。
- 按 S1-S4 分别创建本地原子提交。
- Milestone 末尾集中执行 M1 快速自测并生成 `reports/M1-validation.md`。
- F1 仅修复验收文档和运行证据，不修改生产实现。
- F2 修正当前用户 Host 配置，并补充资源上限文档、最小静态 guard 和 Browser 证据。
- F3 让 create/open 关联 View 首次拉取并加载完整持久化画布；持续编辑与保存仍属于 M2。

## 明确非目标

- 不修改 `dsh-uni-editor`。
- 不实现 M2 的人工 dirty/save/conflict View 状态机。
- 不实现 M3 的元素语义 patch、Ask AI 和 Session 交接。
- 不实现 M4 的资源导入、SVG/PNG 导出与发布收口。
- 不执行 push、PR、amend 或 rebase。

## Release Hardening 清单

- 全仓类型检查、lint、完整测试和生产发布门禁。
- 补充有效模型额度后执行四个真实 DSH Web 工程/画布生命周期 Case。
- 累计 diff 独立 Review 和安全审查。

## 集中自测

- `pnpm typecheck`：PASS。
- 受影响测试：12/12 PASS。
- `pnpm test`：build PASS，15/15 测试 PASS，packed install PASS。
- 完整日志：`.tmp/m1/m1-self-test.log`。
- 验证报告：[`reports/M1-validation.md`](../../reports/M1-validation.md)。

测试结束后，TRAE sandbox 阻止 pnpm 清理 `~/Library/pnpm/_tmp_*`，导致命令
包装层返回 restricted；日志中的构建、packed install 和 15 个断言均已通过。
该环境限制不作为产品失败，普通终端验收仍要求 `pnpm test` 退出码为 0。

## 验收

完整的前置条件、三个仓库的构建命令、实际
`$DSH_HOME/profiles/web/cordis.patch.yml` 配置、启动步骤和四个用户场景见
[`reports/M1-validation.md`](../../reports/M1-validation.md)。

自动化复验在仓库根目录执行：

```bash
pnpm typecheck
pnpm test
git status --short
```

验收项：类型检查、M0 回归、Workspace 安全边界、revision/CAS/幂等、工程与
画布 lifecycle、可信 metadata、app-only Session 绑定、packed install，以及
报告中的 create/multiple-canvas/reopen/conflict 四个真实 DSH Case。

预期结果：类型检查通过；测试为 15/15 PASS；packed Server 可响应
`tools/list`；收口提交后工作树干净。

失败判定：任一类型、构建或测试断言失败；packed Server 无法启动；安全路径、
CAS、幂等、默认画布或 Session 绑定约束失效；真实 DSH Case 的工具结果或
文件系统结果不符；存在非预期未提交文件。

F1 实测已完成 DSH 配置、启动、页面加载、Workspace 选择和工具注册，模型在
首个 tool call 前因 `Insufficient Balance`（HTTP 402、`QUOTA`）终止。该外部
阻塞不记为产品 PASS；M1 继续保持 `AWAITING_ACCEPTANCE`。

F2 实测中，现有 `create_project` 与 `open_project` 工具结果均成功，但 2 MiB
Host 上限拒绝了 8,187,149 B App Resource。将当前 Web profile 的
`maxBodyBytes` 改为 `16777216` 后，PID `57670` 通过配置 HMR 自动恢复，已有
Session 的错误和 loading 文案消失，两个 sandbox iframe 与 fullscreen 控件
可见，Excalidraw `0.18.0` 字体加载成功。未重启 Host、未点击 Retry、未提交新
工具调用。截图接口被限流，且跨域阻止直接 pixel inspection；本结果不证明
持久化画布内容已同步到 M1 View。

F3 修复后，四个 View-producing tools 均在顶层返回 `canvasPath/revision`；
View 在 `app.connect()` 前注册 `app.ontoolresult`，随后调用 app-only
`pull_canvas` 并以官方 `restore()` 加载完整 `elements/appState/files`。
定向类型检查、build 和 4 个测试均通过。当前 build 在同一 Session id 与
Workspace 上执行 `open_canvas -> pull_canvas`，返回 revision `39d588...`
和 92 elements。真实 3080 Host 保持 HTTP 200，并已加载当前 8,188,296 B
Resource。Playwright 精确打开原 Session 后发现 9 个 frame，其中 4 个
Excalidraw hook 均为 `elementCount() = 2`：历史 create/open_project 结果
缺少 F3 新字段，历史 open_canvas 结果在 MCP 进程重连后失去内存绑定。
由于本轮限定两次定向尝试，未再提交新模型调用。最终 build 的协议与数据链
PASS，但真实 DSH 新调用的 92-element 像素验证仍待人工确认，M1 保持
`AWAITING_ACCEPTANCE`。

## 已关闭 Checkpoint

- S1：最终 Phase `commit`，状态 `COMMITTED_LOCAL`，commit `6743241`。
- 验证：`pnpm typecheck`、文件哈希稳定性、`git diff --check` 和 cached diff 检查均 PASS。
- 证据：`.tmp/m1/s1-typecheck.log`。
- Release Hardening：全量门禁、写入故障注入和独立安全审查延后统一执行。
- S2：最终 Phase `commit`，状态 `COMMITTED_LOCAL`，commit `5b6407c`。
- 验证：`pnpm typecheck`、文件哈希稳定性、`git diff --check` 和 cached diff 检查均 PASS。
- Release Hardening：工程 lifecycle 故障注入和更大目录规模验证延后统一执行。
- S3：最终 Phase `commit`，状态 `COMMITTED_LOCAL`，commit `ec8f607`。
- 验证：`pnpm typecheck`、测试文件 `node --check`、文件哈希稳定性、`git diff --check` 和 cached diff 检查均 PASS。
- Release Hardening：跨进程并发和文件系统故障注入延后统一执行。
- S4：最终 Phase `commit`，状态 `COMMITTED_LOCAL`，commit `6cec45c`。
- 验证：`pnpm typecheck`、测试文件 `node --check`、文件哈希稳定性、`git diff --check` 和 cached diff 检查均 PASS。
- Release Hardening：真实 DSH Browser、累计 Review 和发布门禁延后统一执行。

## EXECUTION_CHECKPOINT（已关闭）

- Updated at: 2026-09-03 18:00 +0800
- Milestone: M1 工程与画布生命周期
- Slice: F3 M1 acceptance feedback
- Phase: commit
- Slice state: `COMMITTED_LOCAL`
- Completed facts: 用户 Session 证据确认目标文件包含 92 elements；`open_canvas` 已绑定目标 revision `39d5880025cd7e51bc135ab6f2d8fafa1453c8d19aac3340d0e8952a179f1895`，但旧 View 仍为空白。根因是 M0 View 未处理 invoking tool result。F3 已统一四个 View-producing tool 的顶层 `canvasPath/revision`，并在 `app.connect()` 前注册 `app.ontoolresult`，调用既有 `pull_canvas` 后以官方 `restore()` 加载完整 document。
- Repository state: `/Users/bytedanceo/Workspace/DeepSeekSpace/excalidraw-editor-mcp`，分支 `main`，F3 基线 HEAD `86641f6`；F3 仅持有 `src/server.ts`、`src/view.tsx`、`tests/m1-mcp.test.mjs`、`docs/status/M1.md` 和 `reports/M1-validation.md`。编辑前 SHA-256：`src/view.tsx` `a6d0ba1f...`、`src/server.ts` `34d2f3f8...`、`tests/m1-mcp.test.mjs` `a427b905...`、`docs/status/M1.md` `6706c1c0...`、`reports/M1-validation.md` `c63f6883...`。
- Intended changes: 无；F3 功能候选等待用户视觉验收。
- Explicit exclusions: M2 dirty/save/conflict/polling/statusbar，M3 element tools，其他仓库修改，Release Hardening，push/PR/amend/rebase。
- Verification: 修复前真实 DSH FAIL；`node --test tests/m1-mcp.test.mjs` 红测按预期 FAIL，`create_project.structuredContent.canvasPath` 实际为 `undefined`；实现后 `pnpm typecheck` PASS；`pnpm build` PASS；`node --test tests/m1-mcp.test.mjs tests/m0.test.mjs` 4/4 PASS；当前 build 的目标协议链返回相同 open/pull revision、`changed=true`、`type=excalidraw`、92 elements、4 个 appState keys 和 0 files。3080 Host HTTP 200，当前 Resource 8,188,296 B，包含 `pull_canvas` 与 View test hook。
- Evidence paths: `/Users/bytedanceo/Downloads/dsh-session-session-7063f27b-0dfa-431b-975d-25818c709233 (1)/session.jsonl`；`/Users/bytedanceo/Workspace/DeepSeekSpace/threejs-editor-mcp/tests/fixtures/m6/workspace/excalidraw/default-canvas/main.excalidraw`；`.tmp/m1/f3-red.log`；`.tmp/m1/f3-runtime-target-attempt2.png`；`reports/M1-validation.md`。
- Run identity: Host PID `57670`，readiness `http://127.0.0.1:3080/` HTTP 200，Session `session-7063f27b-0dfa-431b-975d-25818c709233`；最终 build 后 `open_canvas` View `1a57e711-bba3-4284-8403-6d898a51df30`，connection generation `868888f1-ee1a-4a7f-ac3f-1f8306105f2a`。PID `92200` 属于 3094 M0 实例，未触碰。
- Continuity constraints: Host、URL、Session、Workspace 和目标画布保持不变；仅原 Excalidraw MCP phase 因 build 被停止并由 Host 重连。
- Invalidators: Host PID 退出、3080 readiness 失败、Session/Workspace 切换或再次 build 会使对应实时证据失效。
- Blockers and risks: Playwright 已精确选中原 Session，并在 9 个 frame 中读取 4 个 View hook；这些历史结果在最终 build 重启 MCP phase 后不能重新建立 server 内存绑定，故均停在 seed 的 2 elements。截图 `.tmp/m1/f3-runtime-target-attempt2.png` 证明 Session 已打开，但没有可见目标 iframe，不能作为 92 elements 的像素证据。当前实现仍需一次由新 MCP 进程处理的 create/open 调用才能完成真实 DSH 视觉验收。
- Exact next action: 用户在当前 Session 重新调用一次 `open_canvas`，确认关联 View 显示 92-element 画布；无需进入 M2。
- Stop condition: F3 本地原子提交并回传报告后保持 M1 `AWAITING_ACCEPTANCE`；不得进入 M2 或 Release Hardening。
