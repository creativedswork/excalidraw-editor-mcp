# M0 状态

> 当前状态：`ACCEPTED`
>
> 验收日期：2026-09-03

## Slice 台账

| Slice | 结果 | 仓库 | Commit |
|---|---|---|---|
| S1 | 官方 Node API round trip、stdio MCP Server、bundled View | `excalidraw-editor-mcp` | `066f449` |
| S2 Host | JSON/SVG/PNG 的 Host 下载支持 | `dsh-uni-editor` | `68cf501` |
| S2 App | 字体/CSP、inline 高度、真实交互与连续性 | `excalidraw-editor-mcp` | 当前候选提交 |

功能实现耗时为数小时量级；Milestone 集中自测为数十分钟量级。

## 已实现

- 官方 Excalidraw React View 在 MCP App Sandbox 中运行。
- `show_canvas_spike` 通过 stdio MCP Server 打开 Canvas。
- Node bundle 调用官方 conversion、restore 和 serialization API。
- `esm.sh` 字体资源在声明的 CSP 下加载。
- inline/fullscreen 使用同一 iframe，保留 scene 和选择状态。
- pointer 选择与拖动命中正确。
- Host 可下载 `.excalidraw`、SVG 和 PNG。

## 明确非目标

- 工程和画布持久化。
- AI 结构化 scene 编辑工具。
- dirty/save/conflict 状态机。
- 实时多人或 CRDT。
- Library、embeddable 和瞬时 UI 控制工具。

这些能力分别属于 M1-M4。

## 集中自测

- `pnpm test`：官方 API round trip 与 MCP stdio/App Resource 测试。
- 真实 DSH Web：MCP tool -> Resource -> 双 iframe -> Excalidraw。
- Canvas 像素采样、字体检查、pointer 选择和拖动。
- inline -> fullscreen -> inline identity 与选择状态检查。
- JSON/SVG/PNG 浏览器下载及落盘格式检查。

完整证据见 [`reports/M0-validation.md`](../../reports/M0-validation.md)。

## Release Hardening 清单

- DSH 与 MCP 仓库全量类型检查、测试、lint 和生产构建门禁。
- packed install、断网资源、desktop/mobile 回归。
- 下载边界自动化测试与独立累计 diff Review。

当前未执行这些项目，因为 M0 只收敛集成可行性；它们不得在发布前省略。

## 工作区边界

- `.tmp/` 验证产物不提交。
- `dsh-uni-editor` 中与本任务无关的 controller/test 和 promotion/reference
  改动未暂存、未提交。
- 未 push，未创建远端仓库或 PR。

## 验收决策

用户在 M0 报告后选择“继续推进吧”，对应“通过 M0，进入 M1”；M1 已按该
决定启动并完成开发，因此 M0 状态收口为 `ACCEPTED`。

人工步骤、预期证据和失败判定保留在验证报告中。
