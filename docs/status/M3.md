# M3 状态

> 当前状态：`AWAITING_ACCEPTANCE`
>
> 基线：M2 `ACCEPTED`，closure commit `72e3d3b`
>
> 功能 HEAD：`d7a7f7b`
>
> 目标：AI 结构化编辑与 Session 交接

## Slice 台账

| Slice | 状态 | 结果 | Commit |
|---|---|---|---|
| S1 | `COMMITTED_LOCAL` | 有界语义 `inspect_canvas` | `8b7065b` |
| S2 | `COMMITTED_LOCAL` | 原子 `apply_canvas_changes` 基础 | `c888eb1` |
| S3 | `COMMITTED_LOCAL` | V1 关系操作与引用修复 | `fafca9b` |
| S4 | `COMMITTED_LOCAL` | `replace_canvas`、Prompt 与 Ask AI | `2f35ba2` |
| S5 | `COMMITTED_LOCAL` | DSH sandbox retained-link 安全归一化 | `c76efd2` |
| S6 | `COMMITTED_LOCAL` | 模型可见的 canonical path、revision 与结构化结果 | `d7a7f7b` |

补充文档提交：`c610bca`、`b9f301f`。本次仅形成最终 docs-only closure commit，
不修改产品实现。

## 范围

- 只修改 `excalidraw-editor-mcp`。
- 完成 M3 的语义 inspect、原子批量编辑、关系操作、整画布替换和 Ask AI Session
  交接。
- 使用 commit `d7a7f7b` 的 detached candidate 和真实
  `m3-anthropic/deepseek-v4-pro` 完成单一双画布 storyboard。
- 接任 Owner 沿用 durable checkpoint；未重跑产品实现、route probe、单元测试、
  typecheck 或 build。

## 明确非目标

- 不实现 M4 asset import/export 或网络图片。
- 不修改其他仓库或 DeepSeek Harness Agent Loop。
- 不访问或修改 `default-canvas`。
- 不执行 push、PR、amend、rebase 或发布。

## 集中验证

- M3 focused self-test：8/8 PASS，证据 `.tmp/m3/m3-quick-self-test.log`。
- S5 store regression：5/5 PASS。
- `pnpm typecheck`：PASS。
- detached production build 和 main production build：PASS；仅有既有
  `EMPTY_IMPORT_META` warning。
- S6 `node --test tests/m3-mcp.test.mjs`：4/4 PASS；覆盖 lifecycle、inspect、
  mutation 的 model-visible JSON 和完整 workspace-relative `canvasPath`。
- route probe：真实模型精确返回 `M3_ROUTE_PROBE_OK`。
- 功能开发以多小时计；集中自动化和最终 runtime 验证以分钟计，明显短于开发时间。
- 以上实现门禁来自既有 checkpoint；本次按要求未重复执行。

## 最终 Runtime Storyboard

- Origin：`http://127.0.0.1:3102`
- Candidate：`.playwright-mcp/m3-s6-runtime-d7a7f7b-20260904/candidate`
- Candidate HEAD：`d7a7f7b4ef757799f64d61e3a9e3a6227a86c7c0`
- Host PID：`15907`
- Candidate MCP PID：`18227`
- Runtime PID：`60191`
- Session：`session-d5b672a8-6836-47b3-adbd-b080755f9c15`
- Project：`excalidraw/m3-smoke-1788497278546-60191`
- Browser：fresh persistent profile
- Transport：真实模型，无 fixture、mock transport、synthetic click 或 test hook

同一 run 通过以下断言：

1. 创建 managed project、`main.excalidraw` 和 `deployment.excalidraw`。
2. main 初始为 Clean，包含带 `API`、`Database` label 的两个 rectangle 和一条
   start/end 均绑定到节点的 arrow；元素数为 5。
3. 人工键盘移动节点后状态变为 Dirty；Save 后回到 Clean，选择仍保留，revision 从
   `a23f43d1...` 前进到 `fb6903ac...`。
4. Ask AI 发送普通 Session 消息；模型先 `inspect_canvas`，再且仅再执行一个
   `apply_canvas_changes` batch，增加 `Queue` 并重排节点。
5. AI 更新后元素数从 5 增至 7，revision 前进到 `bf7c825c...`，View instance
   `60b239c6-7c3d-4ce7-b017-f05b7fa96a1b` 不变，所选节点
   `2f8050b8-8e63-40c8-a37b-e39937dcdbb8` 保留。
6. 同一 Session 通过 main View 的 Ask AI 打开 deployment；人工绘制 rectangle
   并 Save，最终为 Clean/Saved，元素数为 1，revision 为 `2bfbf918...`。

`runtime-results.json`、Session trace、可见 tool trace、两份最终画布和五张截图均来自
runtime PID `60191`。PTY 最后只报告 Chrome Crashpad 全局 settings 文件的 sandbox
拒绝；完整结果文件已在该提示前写入，runtime 与 browser 随后均退出。

## GIF

- 输出：
  `.playwright-mcp/m3-s5-final-c76efd2-20260904/evidence/m3-s6-storyboard.gif`
- 编码：5 个 source frames、150 个 encoded frames、1200x834、10 fps、15.0 秒、
  319798 bytes、128 colors。
- SHA-256：
  `3d9dfa7bfd07ace67db93daef934f6b2fc4b99e6b653026546fd40859256d210`
- 已解码并目视检查五个代表帧：
  `.playwright-mcp/m3-s5-final-c76efd2-20260904/evidence/gif-check/00.png`
  至 `04.png`。
- GIF 未发布、未推送、未附加到 PR。

## Evidence

根目录：
`.playwright-mcp/m3-s5-final-c76efd2-20260904/evidence/`

- `runtime-results.json`
- `tool-trace.txt`
- `session-d5b672a8-6836-47b3-adbd-b080755f9c15.jsonl.zstd`
- `final-main.excalidraw`
- `final-deployment.excalidraw`
- `m3-s6-storyboard.gif`
- `provenance.md`
- `gif-check/00.png` 至 `gif-check/04.png`
- 同级 `../frames/00-project-ready.png` 至 `../frames/04-second-canvas-saved.png`

保留的失败诊断均与最终证据隔离，包括
`semantic-count-failure-session-4d500803/`、
`composer-selection-failure-runtime-19335/`、
`human-selection-failure-runtime-23997/`、
`post-ai-composer-selector-failure-runtime-26548/`、
`idempotency-replay-failure-session-96ace15d-runtime-31241/`、
`view-timeout-session-d5a87330-runtime-41022/`、
`disconnected-frame-selection-runtime-46321/`、
`deployment-ask-ai-viewport-failure-runtime-48424/` 和
`deployment-centered-scroll-failure-runtime-54732/`。这些目录不参与最终 GIF。

## Cleanup

- Runtime PID `60191` 与其 browser 已退出。
- M3 Host process group `15810` 已停止；Host PID `15907`、candidate PID
  `18227` 和 3102 listener 均不存在。
- 无 `runtime.mjs` 或使用本 storyboard profile 的 Chrome 进程存活。
- 无关 3097 listener PID `37490` 保持存活且未触碰。
- `.playwright-mcp/` evidence 为 gitignored，不进入 closure commit。

## 人工验收

1. 复核 S1-S6 commits 和上述既有自动化验证。
2. 打开 `m3-s6-storyboard.gif`，确认五个状态按顺序展示 project-ready、人工
   save/selection、Ask AI 请求、AI 完成轨迹和 deployment Saved。
3. 对照 `runtime-results.json`，确认 main 元素数 `5 → 7`、revision 前进、
   instance ID 不变且 selected ID 保留。
4. 对照 `tool-trace.txt` 与压缩 Session trace，确认 AI 回合先 inspect，再执行
   exactly one apply batch，并返回 `M3_AI_DONE`。
5. 检查 `final-main.excalidraw` 包含 `API`、`Database`、`Queue` 和有效 bound
   arrow；检查 `final-deployment.excalidraw` 含一个人工创建的 rectangle。

预期结果：两个 canvas 均 Saved；main 的结构和关系有效；AI 更新不替换 View、
不丢失 surviving selection；全部最终证据同源且不含 secret。

失败判定：batch 部分写入，CAS/幂等或引用修复失效，dirty Ask AI 可发送，模型未按
inspect/apply 顺序执行，AI 更新替换 View 或丢失选择，deployment 未保存，证据/GIF
缺失或混用不同 run。

## Release Hardening 清单

- 全仓测试、全量 lint、覆盖率扩展、完整生产发布门禁和独立 Review。
- 大文档与复杂官方元素组合的压力及兼容性矩阵。
- 长时间 polling、多 View 并发、断线重连和长期 Session 耐久性。
- 不同真实模型/provider 的工具调用稳定性与 `mutationId` 生成策略。
- 浏览器宿主存在多个历史 View 时的资源回收和可见性回归。

## Slice Reports

### S1

SLICE_REPORT
- 完成结果：`inspect_canvas` 支持 ID/type/text 过滤、有界 cursor 分页、截断标记和关系投影。
- 修改文件：`src/canvas-operations.ts`、`src/server.ts`、`tests/m3-canvas-operations.test.mjs`、`docs/status/M3.md`。
- 基础检查：红测 3/3 后 green 3/3；`pnpm typecheck`、`git diff --check` PASS。

### S2

SLICE_REPORT
- 完成结果：`apply_canvas_changes` 支持原子 CAS、幂等、clientRef 和基础语义操作。
- 修改文件：`src/official.ts`、`src/project-store.ts`、`src/server.ts`、`tsdown.config.ts`、`tests/m3-mcp.test.mjs`、`docs/status/M3.md`。
- 基础检查：S2 green 1/1；`pnpm typecheck`、`git diff --check` PASS。

### S3

SLICE_REPORT
- 完成结果：完成 reorder、group/ungroup、bind/unbind、Frame 操作和删除引用修复。
- 修改文件：`src/official.ts`、`src/server.ts`、`tests/m3-mcp.test.mjs`、`docs/status/M3.md`。
- 基础检查：关系 green 1/1，M3 MCP 组合检查 2/2；`pnpm typecheck`、`git diff --check` PASS。

### S4

SLICE_REPORT
- 完成结果：完成 `replace_canvas`、`excalidraw-authoring` Prompt 和 saved-only Ask AI Session 消息。
- 修改文件：`src/official.ts`、`src/project-store.ts`、`src/server.ts`、`src/view-state.ts`、`src/view.tsx`、`src/styles.css`、`tests/m3-mcp.test.mjs`、`tests/m3-view-state.test.mjs`、`docs/status/M3.md`。
- 基础检查：replace/prompt 1/1、Ask AI 2/2、隔离 builds、`pnpm typecheck`、`git diff --check` PASS。

### S5

SLICE_REPORT
- 完成结果：合法 sandbox-retained publication link 被安全归一化，后续 user hardlink 仍被拒绝。
- 修改文件：`src/canvas-store.ts`、`tests/m1-store.test.mjs`、`docs/status/M3.md`。
- 基础检查：store regression 5/5、`pnpm typecheck`、`pnpm run build`、`git diff --check` PASS。

### S6

SLICE_REPORT
- 完成结果：MCP lifecycle、inspect 和 mutation 结果向模型暴露 bounded
  machine-readable JSON，并强化 canonical `canvasPath` 契约。
- 修改文件：`src/server.ts`、`tests/m3-mcp.test.mjs`。
- 基础检查：M3 MCP 4/4、`pnpm typecheck`、`pnpm run build` PASS；最终真实模型
  storyboard 通过。

## EXECUTION_CHECKPOINT

- Updated at: 2026-09-04 12:54 CST
- Milestone: M3 AI 结构化编辑与 Session 交接
- Slice: S6 model-visible MCP result contract
- Phase: `acceptance-ready`
- Slice state: `COMMITTED_LOCAL`
- Completed facts: S1-S6 已提交；最终真实模型双画布 storyboard、五帧 GIF 编码与
  encoded-frame 目视检查均完成。
- Repository state: branch `main`，功能 HEAD `d7a7f7b`；仅两份 M3 closure 文档
  待提交，ignored evidence 不入 Git。
- Explicit exclusions: M4、Release Hardening、其他仓库、`default-canvas` 和所有
  远端操作。
- Verification: 既有 focused tests/typecheck/build/route probe PASS；runtime
  PID `60191` 的 `runtime-results.json` 与 final canvas 文件 PASS。
- Evidence paths: 本文 `Evidence` 节所列路径。
- Run identity: final Session `session-d5b672a8-6836-47b3-adbd-b080755f9c15`；
  runtime/browser/Host/candidate 均已停止，证据已固化。
- Blockers and risks: 无 M3 acceptance blocker；Release Hardening 清单仍未执行。
- Exact next action: 用户审阅本状态、验证报告和 GIF，并决定接受 M3 或继续迭代。
- Stop condition: 停在 `AWAITING_ACCEPTANCE`；未获明确验收前不进入 M4。
