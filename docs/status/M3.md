# M3 状态

> 当前状态：`AWAITING_ACCEPTANCE`
>
> 基线：M2 `ACCEPTED`，closure commit `72e3d3b`
>
> 目标：AI 结构化编辑与 Session 交接

## Slice 台账

| Slice | 状态 | 结果 | Commit |
|---|---|---|---|
| S1 | `COMMITTED_LOCAL` | 有界语义 `inspect_canvas` | `8b7065b` |
| S2 | `COMMITTED_LOCAL` | 原子 `apply_canvas_changes` 基础 | `c888eb1` |
| S3 | `COMMITTED_LOCAL` | V1 关系操作与引用修复 | `fafca9b` |
| S4 | `COMMITTED_LOCAL` | `replace_canvas`、Prompt 与 Ask AI | `2f35ba2` |

## 范围

- 只修改 `excalidraw-editor-mcp`。
- 按已批准的 S1-S4 顺序实现 M3。
- 复用 `ProjectStore`、`CanvasStore` 和官方转换/恢复 API。
- 所有 Slice 提交后只执行一次 M3 集中快速自测和真实 DSH 人机回合。

## 明确非目标

- 不实现 M4 asset import/export/release hardening 或网络图片。
- 不修改其他仓库或 deepseek-harness Agent Loop。
- 不执行 push、PR、amend 或 rebase。
- 不触碰用户 92-element 画布。

## Release Hardening 清单

- 全仓测试、全量 lint、生产发布门禁、覆盖率扩展和独立 Review。
- 大文档与复杂官方元素组合的压力及兼容性矩阵。
- 长时间 polling、多 View 并发和断线耐久性。
- 修复原 Host 的 provider `1210` 错误后，重跑一次完整人机双画布回合，
  保留 tool trace、最终画布、截图并编码和目视核验 GIF。

## 集中验证

- 功能开发以多小时计；集中 quick self-test 约 3 秒，验证时间明显小于开发时间。
- `node --test tests/m3-canvas-operations.test.mjs tests/m3-mcp.test.mjs tests/m3-view-state.test.mjs`：
  8/8 PASS。
- `pnpm typecheck`：PASS。
- clean detached worktree 在 commit `2f35ba2` 上 production build：PASS。
- main checkout 在功能 HEAD `2f35ba2` 上重新 production build：PASS。
- build 仅报告既有 `EMPTY_IMPORT_META` warning。
- 完整结果和证据索引见 `reports/M3-validation.md`。

## 运行时验证

- 隔离 Host `3096` 达到 HTTP 200，但 `session.create` 重复报
  `internal: signal timed out`，随后 browser reload 超时；按停止条件未再重试。
- 经用户批准，fallback 复用原 Host PID `57670`、fixture Workspace 和已验证
  browser profile。main build 后只重启 Excalidraw MCP 子进程
  `17015` -> `11695`。
- fallback 成功创建 fresh Session
  `session-3ab217b0-b317-40da-9183-01f6c8acb0d9` 并写入用户消息。
- 当前原 Host 模型路由在任何 MCP tool call 前返回 HTTP 400、provider code
  `1210`、`INVALID_REQUEST`。因此未创建 `excalidraw/m3-smoke`。
- `default-canvas` 未访问或修改；原 Host 停止验证后仍返回 HTTP 200。
- 只保留请求态截图，不存在可接受的完成截图序列、tool trace、最终画布或 GIF。

## 人工验收

1. 复核 S1-S4 四个本地 commit 和 M3 focused tests 8/8 结果。
2. 验证 Saved Ask AI 只发送一条 Session 消息，dirty/conflict 状态不能发送。
3. provider 恢复后在 `excalidraw/m3-smoke` 完成一次人机双画布回合。
4. 确认 AI 更新不替换 iframe、revision 前进并保留 surviving selection。
5. 保留 tool trace、最终画布、完整截图和目视核验后的 GIF。

失败判定：自动化定向测试回归，批次部分写入，CAS/幂等失效，引用修复失效，
dirty Ask AI 可发送，clean AI 更新丢失 View/选择，或实际模型回合仍无法到达 MCP。

## Slice Reports

### S1

SLICE_REPORT
- 完成结果：`inspect_canvas` 支持 ID/type/text 过滤、有界 cursor 分页、结果与文本截断标记，并将容器绑定文本折叠为保留底层 ID 的 label；结果包含 bounds、style、group、frame 和 bindings。
- 修改文件：`src/canvas-operations.ts`（语义投影和 cursor）；`src/server.ts`（工具输入与小文档上限）；`tests/m3-canvas-operations.test.mjs`（S1 红绿检查）；`docs/status/M3.md`（累计状态）。
- 基础检查：缺失实现时 3/3 红；实现后 `node --test tests/m3-canvas-operations.test.mjs` 3/3 PASS，`pnpm typecheck` PASS，`git diff --check` PASS。

### S2

SLICE_REPORT
- 完成结果：`apply_canvas_changes` 通过官方 skeleton conversion、restore 和 version helper 原子执行 add/update/remove/set_canvas；同批 `clientRef` 可用于箭头关系并返回稳定映射；整批只做一次 CAS 写入，支持 exact retry 幂等并拒绝 mutationId 不同输入复用。
- 修改文件：`src/official.ts`（官方批量转换）；`src/project-store.ts`（幂等与单次 CAS）；`src/server.ts`（语义 schema 和工具）；`tsdown.config.ts`（Node server 打包官方依赖）；`tests/m3-mcp.test.mjs`（S2 MCP 红绿检查）；`docs/status/M3.md`（累计状态）。
- 基础检查：旧 bundle 上 S2 红测 1/1 FAIL；隔离 server bundle 后 S2 green 1/1 PASS，覆盖九种 add、existing file image、update/remove/set_canvas、clientRef bindings、invalid atomic batch、stale revision 和幂等 mismatch；`pnpm typecheck` PASS，`git diff --check` PASS。

### S3

SLICE_REPORT
- 完成结果：完成 reorder、group/ungroup、bind/unbind、add_to_frame/remove_from_frame；remove 统一修复容器文本、线性绑定反向引用和 Frame 成员，最终经官方 restore/serialize 保持 version、index 与 binding 不变量。
- 修改文件：`src/official.ts`（关系操作和删除修复）；`src/server.ts`（关系操作 schema）；`tests/m3-mcp.test.mjs`（S3 红绿关系检查）；`docs/status/M3.md`（累计状态）。
- 基础检查：S2 bundle 上关系红测 1/1 FAIL；隔离 S3 server build PASS；关系 green 1/1 PASS，M3 MCP 组合检查 2/2 PASS；`pnpm typecheck` PASS，`git diff --check` PASS。

### S4

SLICE_REPORT
- 完成结果：`replace_canvas` 在单次幂等 CAS 写入前执行 raw schema/size、元素引用和官方 restore 校验，并保留固定版本可序列化字段；注册 `excalidraw-authoring` Prompt；View 增加 saved-only Ask AI 输入与普通 `ui/message`，消息包含 canvasPath、revision、选择和用户文本。
- 修改文件：`src/official.ts`（完整文档引用/restore 校验）；`src/project-store.ts`（replace 幂等 CAS）；`src/server.ts`（replace tool 与 Prompt）；`src/view-state.ts`（Ask AI 门禁和消息）；`src/view.tsx`、`src/styles.css`（状态栏 Ask AI UI）；`tests/m3-mcp.test.mjs`、`tests/m3-view-state.test.mjs`（S4 红绿检查）；`docs/status/M3.md`（累计状态）。
- 基础检查：replace red 1/1 与 Ask AI red 2/2 按预期失败；replace/prompt green 1/1、Ask AI green 2/2、隔离 server/View build 和 `pnpm typecheck` PASS；View build 仅有既有 `EMPTY_IMPORT_META` warning；`git diff --check` PASS。

## EXECUTION_CHECKPOINT（已关闭）

- Updated at: 2026-09-04
- Milestone: M3 AI 结构化编辑与 Session 交接
- Final phase: `MILESTONE_CANDIDATE -> AWAITING_ACCEPTANCE`
- Slice state: S1-S4 均为 `COMMITTED_LOCAL`
- Commits: `8b7065b`、`c888eb1`、`fafca9b`、`2f35ba2`
- Repository state: `main`，功能 HEAD `2f35ba2`；最终只提交 STATUS 和 validation report。
- Verification: focused tests 8/8、typecheck、detached build 和 main build PASS；
  `git diff --check` PASS。
- Runtime result: 隔离 Host 被 `session.create` 超时阻断；原 Host fallback 成功创建
  fresh Session，但模型在 MCP 前返回 provider `1210`，未生成 M3 fixture 或完成证据。
- Retained evidence: `.tmp/m3/`、`.playwright-mcp/m3-runtime/evidence/`、
  `.playwright-mcp/gif-frames-m3-3080/00-create-request.png`、
  `reports/M3-validation.md`。
- Remaining Release Hardening: 修复模型路由后补实际双画布回合、tool trace、
  最终画布、完整截图和 GIF；再执行全量门禁及独立 Review。
- Explicit exclusions: M2 测试重跑、M4、其他仓库、`default-canvas` 和远端操作。
- Next action: 用户决定接受 M3、继续迭代，或保留当前候选。
