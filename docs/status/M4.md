# M4 状态

> 当前状态：`IMPLEMENTING_SLICES`
>
> 基线：M3 `ACCEPTED`，closure commit `13d9021`
>
> 目标：资源、导出与发布

## Slice 台账

| Slice | 状态 | 结果 | Commit |
|---|---|---|---|
| S1 | `COMMITTED_LOCAL` | 有界本地图片导入与 live-reference 清理 | `63638bb` |
| S2 | `COMMITTED_LOCAL` | Browser 官方 API + Host download-file 导出 | `a230bdc` |
| S3 | `COMMITTED_LOCAL` | Distribution/readiness | this commit |
| S4 | `PENDING` | Packed DSH runtime validation | pending |

## 范围

- 只修改 `excalidraw-editor-mcp`。
- 实现有界 Workspace 本地图片导入、未引用资源清理和 JSON/SVG/PNG 下载。
- 完成 npm pack 可安装性、发布文档与 M4 明确要求的真实 DSH Web 验证。

## 明确非目标

- 不抓取网络图片，不生成 AI 图片。
- 不实现 CRDT、协作或 DSH Agent Loop 变更。
- 不修改其他仓库或 `default-canvas`。
- 不执行独立 Review、全量跨里程碑加固、push、PR、amend、rebase、publish 或公开仓库操作。

## Release Hardening 清单

- M0-M4 累计 diff 的独立代码审查和额外覆盖。
- 超出 M4 明确退出条件的完整跨版本、跨 provider 和长期耐久性矩阵。

## S1 Asset lifecycle

- `add_canvas_asset` 仅接收当前 DSH Workspace 内的 PNG/JPEG/GIF/WebP；拒绝
  URL、越界路径、symlink、hardlink 和无效图片头。
- 单资源上限 1 MiB，解码后资源总量上限 2 MiB，宽高均不超过 8192，像素总量
  不超过 32 MiPixels；最终文档继续受 canonical 4 MiB 上限保护。
- 内容 SHA-256 同时作为稳定 `assetId` 和返回 hash；结果包含 canonical
  `canvasPath`、MIME、宽高、字节数和 revision。
- `remove_unused_assets` 只删除未被任何 live image element 引用的文件。
- 两项变更都复用 ProjectStore mutation registry、串行队列和 CanvasStore 单次
  CAS atomic write；精确重试返回首个结果。

## S2 Export lifecycle

- `export_canvas` 返回 canonical canvas path、revision、format、安全派生 filename
  和 `ui/download-file` delivery，并绑定唯一的 export View。
- JSON 使用官方 serializer；SVG/PNG 只在 Browser View 中调用固定版本
  Excalidraw `exportToSvg` / `exportToBlob`，再交给 Host 下载。
- 既有人工 JSON/SVG/PNG 按钮复用同一实现和 canvas-derived filename。
- View 拉取的 revision 与 export 请求不一致时拒绝下载并要求重试。
- DESIGN 明确记录 `export_canvas` 是 lifecycle-only View 绑定规则的唯一窄例外。

## EXECUTION_CHECKPOINT

- Updated at: 2026-09-05 02:17 CST
- Milestone: M4 资源、导出与发布
- Slice: S3 Distribution/readiness
- Phase: `commit`
- Slice state: `COMMITTED_LOCAL`
- Completed facts: S1 `63638bb`、S2 `a230bdc` 已提交；恢复核验确认 branch/HEAD
  和空暂存区匹配；S3 已补齐双语 README、许可与第三方声明、示例配置、CI、
  release scripts 和 packed/offline assertions；字体嵌入构建 PASS，生成 235 个
  WOFF2 data URL，`view.js + style.css` 为 25,664,494 bytes。
- Repository state: `/Users/bytedanceo/Workspace/DeepSeekSpace/excalidraw-editor-mcp`，
  branch `main`，pre-commit HEAD `a230bdc`；S3 目标文件待原子提交。
- Intended changes: 无；S3 实现和基础检查已完成。
- Explicit exclusions: 其他仓库、`default-canvas`、网络图片、AI 图片、协作、
  DSH Agent Loop、独立 Review、远端操作和发布。
- Verification: S3 `pnpm run build` PASS；focused packed install 1/1 PASS；
  `node --check` 与 `git diff --check` PASS。构建只存在既有 `EMPTY_IMPORT_META`
  warning。
- Evidence paths: `.tmp/m4/s3-build.log`、`.tmp/m4/s3-packed-test.log`、
  `docs/status/M4.md`。
- Run identity: N/A；S4 前不建立 browser/runtime identity。
- Continuity constraints: 每个 Slice 只提交自身文件；M4 runtime 证据必须来自单一
  packed candidate 和单一 fresh DSH run。
- Invalidators: branch/HEAD 非预期变化、目标文件哈希漂移、未知 staged 文件或 S4
  candidate/runtime identity 改变。
- Blockers and risks: 当前无 blocker；版本与 private 状态保持 `0.0.0` / `true`
  作为 publish guard；32 MiB Host limit 由 25.66 MB 自包含静态资源实测决定。
- Exact next action: 显式暂存 S3 文件、核对 cached diff 并创建原子 commit。
- Stop condition: commit 回读成功后立即发出 `SLICE_REPORT` 并进入 S4。
