# M2 状态

> 当前状态：`ACCEPTED`
>
> 基线：M1 `ACCEPTED`，commit `27fa16f`
>
> 目标：可持续人工编辑

## Slice 台账

| Slice | 状态 | 结果 | Commit |
|---|---|---|---|
| S1 | `COMMITTED_LOCAL` | 完整本地草稿与持久内容摘要；瞬时 UI 变化不标记 Dirty | `b8c1b55` |
| S2 | `COMMITTED_LOCAL` | 状态栏、显式 Save、Reload、CAS Conflict 和 Save as copy | `adf7719` |
| S3 | `COMMITTED_LOCAL` | 低频同步、无 history 应用、草稿冲突保护和重连恢复 | `6c6237f` |
| S4 | `COMMITTED_LOCAL` | 有界 model context、程序化更新屏障、surface/reconnect 和真实 Browser 验收 | `2817236` |

## 范围

- 只修改 `excalidraw-editor-mcp`。
- View 维护完整本地草稿，只有真实持久内容变化进入 Dirty。
- Save、Reload、Save as copy 复用现有 app-only tools。
- clean 画布低频同步外部 revision；dirty 画布保留草稿并进入 Conflict。
- Save 后提交有界 model context，并验证同一 iframe 的 surface 连续性。

## 明确非目标

- 不实现 `apply_canvas_changes`、`replace_canvas` 或元素语义工具。
- 不实现 Ask AI Session 消息交接，也不放置无功能按钮。
- 不实现 asset/export 发布收口。
- 不修改 `dsh-uni-editor`、`deepseek-harness` 或 `threejs-editor-mcp`。
- 不执行 push、PR、amend 或 rebase。

## Release Hardening 清单

- 全仓 lint、完整发布门禁、累计 diff 独立 Review 和安全审查。
- 大型图片和接近文档上限的浏览器编辑压力测试。
- 长时间 polling、重复断线和多个并发 View 的耐久性验证。

## Slice Reports

### S1

SLICE_REPORT
- 完成结果：View 首次加载完整标准 document；`onChange` 只更新本地草稿，elements、files 或官方持久 appState 变化才进入 Dirty，selection/viewport 等瞬时变化保持 Clean，未触发任何自动保存。
- 修改文件：`src/view-state.ts`（持久内容摘要与编辑状态转换）；`src/view.tsx`（接入本地草稿、dirty 判定和测试 hook）；`tests/m2-view-state.test.mjs`（S1 红测与定向回归）；`docs/status/M2.md`（累计状态和 checkpoint）。
- 基础检查：红测按预期 3/3 FAIL（模块缺失）；实现后 `node --test tests/m2-view-state.test.mjs` 3/3 PASS，`pnpm typecheck` PASS，`git diff --check` PASS。

### S2

SLICE_REPORT
- 完成结果：窄状态栏展示 canvas path 与 Loading/Saved/Dirty/Saving/Conflict；Save 携带 baseRevision、mutationId 和完整草稿，CAS 冲突保留本地草稿；Reload 显式放弃草稿；Save as copy 将冲突草稿保存到用户指定的新画布。
- 修改文件：`src/view-state.ts`（显式动作状态转换）；`src/view.tsx`（状态栏及 push/reload/save-copy 调用）；`src/styles.css`（窄状态栏样式）；`tests/m2-view-state.test.mjs`（状态转换红测）；`tests/m2-mcp.test.mjs`（真实 stdio CAS/文件结果）；`docs/status/M2.md`（累计状态）。
- 基础检查：S2 红测按预期 1/4 FAIL（动作转换不存在）；实现后定向测试 5/5 PASS，`pnpm typecheck` PASS，`git diff --check` PASS。

### S3

SLICE_REPORT
- 完成结果：View 每 5 秒携带 currentRevision 低频 pull；clean 时以 `captureUpdate: NEVER` 更新同一实例并保留仍存在的选择、scroll 和 zoom，dirty 时不覆盖草稿并进入 Conflict；Session binding 使用权限受限的本地记录跨 MCP Server 子进程重启恢复。
- 修改文件：`src/view-state.ts`（外部更新决策和选择/viewport 合并）；`src/view.tsx`（polling、无 history scene 更新和断线重试）；`src/server.ts`（持久 Session binding 恢复）；`tests/m2-view-state.test.mjs`（外部更新策略红测）；`tests/m2-mcp.test.mjs`（跨 Server 进程重启测试）；`docs/status/M2.md`（累计状态）。
- 基础检查：S3 红测按预期 3/8 FAIL；实现后 `pnpm typecheck` PASS，状态测试 6/6 PASS，临时 bundle 上的 stdio 测试 2/2 PASS，`git diff --check` PASS。

### S4

SLICE_REPORT
- 完成结果：Save 成功后提交有界 canvasPath/revision/selection/state model context；initialData、Reload 和 clean external update 使用 Loading barrier，并在 RAF 从 Excalidraw API 建立 canonical baseline，程序化更新保持 Clean；sandbox-safe Save as copy 生成同目录时间戳副本；真实 DSH 验证了 pointer/keyboard/undo/redo/save/reload、clean external、Conflict 草稿保护、copy、surface 连续性、MCP 子进程重启恢复和 mobile 布局。
- 修改文件：`src/view-state.ts`（默认化摘要、Loading barrier 协调、copy path 和 model context）；`src/view.tsx`（canonical hydration/apply、model context、surface hook 和 sandbox-safe copy）；`tests/m2-view-state.test.mjs`（initial/external 多回调、copy path 和 model context 回归）；`reports/M2-validation.md`（集中验证记录）；`docs/status/M2.md`（累计 checkpoint）。
- 基础检查：程序化更新与 copy path 均先红后绿；`pnpm typecheck` PASS；M2 定向测试 13/13 PASS；production build PASS；`git diff --check` PASS；真实 3080 Browser 路径和三张截图 PASS。

## EXECUTION_CHECKPOINT

- Updated at: 2026-09-04
- Milestone: M2 可持续人工编辑
- Slice: S4 Host 协作与运行验收
- Phase: accepted
- Slice state: `COMMITTED_LOCAL`
- Completed facts: 用户已明确接受 M2；S1-S4 分别提交为 `b8c1b55`、`adf7719`、`6c6237f`、`2817236`；M2 集中验证和真实 DSH Browser smoke 证据保持在 `reports/M2-validation.md`。
- Repository state: `/Users/bytedanceo/Workspace/DeepSeekSpace/excalidraw-editor-mcp`，分支 `main`，功能 HEAD `2817236`；本次只等待提交 M2 验收结论。
- Intended changes: `docs/status/M2.md` 的验收收口。
- Explicit exclusions: Ask AI 消息、M3/M4、其他仓库修改、Host PID `57670` 停止、远端操作。
- Verification: `pnpm typecheck` PASS；`node --test tests/m2-view-state.test.mjs tests/m2-mcp.test.mjs` 13/13 PASS；production build PASS；`git diff --check` PASS；Host PID `57670` 和 HTTP 3080 保持；desktop inline/fullscreen 与 390×844 mobile 截图已核验。
- Evidence paths: `reports/M2-validation.md`、`.tmp/m2/m2-quick-self-test.log`、`.tmp/m2/runtime-evidence/runtime-affected.json`、`.tmp/m2/runtime-evidence/desktop-inline.png`、`.tmp/m2/runtime-evidence/desktop-fullscreen.png`、`.tmp/m2/runtime-evidence/mobile.png`。
- Run identity: Host PID `57670`，重连 MCP PID `14938`→`17015`，URL `http://127.0.0.1:3080/`；隔离工程 `excalidraw/m2-smoke` 已清理。
- Continuity constraints: M2 保持冻结；用户 92-element 画布和其他仓库现有改动未触碰；不执行远端操作。
- Invalidators: 重写 M2 历史或修改已验收的 M2 功能范围。
- Blockers and risks: 无 blocker；长时间 polling、多 View 并发和发布全门禁保留到 Release Hardening。
- Exact next action: 创建 docs-only M2 closure commit，然后建立 M3 checkpoint。
- Stop condition: M2 closure commit 完成。
